name: workshop-doc-content-sync
base: ubuntu@24.04
sdks:
  - name: opencode
    channel: latest/stable

actions:
  opencode: |
    source /project/.env
    opencode "$@"
  opencode-run: |
    source /project/.env
    opencode run -m openrouter/deepseek/deepseek-v4-flash "$@"